{
  "name": "@packages/dal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "echo dev: dal",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo lint: dal",
    "generate:dev": "drizzle-kit generate --config=drizzle.config.ts",
    "migrate:dev": "drizzle-kit migrate --config=drizzle.config.ts",
    "generate:prod": "drizzle-kit generate --config=drizzle.prod.config.ts",
    "migrate:prod": "drizzle-kit migrate --config=drizzle.prod.config.ts"
  },
  "dependencies": {
    "@paralleldrive/cuid2": "^3.0.4",
    "dotenv": "^17.2.3",
    "drizzle-orm": "^0.44.7",
    "postgres": "^3.4.7"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.6",
    "typescript": "^5.9.3"
  }
}
